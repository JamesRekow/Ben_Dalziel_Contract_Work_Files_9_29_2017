#  James Rekow

rickerCohortCreator  = function(M, univ = TRUE){
  
  #  ARGS: M - number of samples in cohort
  #        univ - boolean indicating whether the cohort has universal dynamics or not. If it does, then
  #               each parameter is chosen once for the whole cohort. If it doesn't, each parameter is
  #               chosen separately for each individual sample
  #
  #  RETURNS:
  
  
  
} #  end  rickerCohortCreator  function
